interface NavigationBarProps {}
import { Icon } from "../profile/page";

interface NavigationBarProps {
  items: Icon[];
}
export default function NavigationBar({ items }: NavigationBarProps) {
  // items.map((icon) => {
  //   console.log(icon.altText);
  // });
  return <div>{/* //loop button image */}</div>;
}
